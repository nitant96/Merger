<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="gov.cipam.gi.activities.AboutActivity">

    <include
        android:id="@+id/about_activity_toolbar"
        layout="@layout/toolbar" />

    <ImageView
        android:id="@+id/about_activity_image_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:ignore="ContentDescription"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/about_activity_toolbar"
        app:srcCompat="@drawable/common_google_signin_btn_icon_dark" />

    <TextView
        android:id="@+id/about_activity_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="false"
        android:layout_below="@+id/about_activity_image_view"
        android:text="@android:string/VideoView_error_text_invalid_progressive_playback"
        android:textAllCaps="true" />

</RelativeLayout>
